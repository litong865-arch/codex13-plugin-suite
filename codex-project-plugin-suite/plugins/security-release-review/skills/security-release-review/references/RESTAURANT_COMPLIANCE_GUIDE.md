# Restaurant Compliance Guide

餐饮商家项目必须检查：

- `medium` 和 `high` 风险评论必须人工审核。
- 食品安全、退款、投诉、敏感内容禁止全自动发布。
- 未授权平台 `postReply()` 必须失败或禁用。
- 前端套餐开关只负责展示，后端必须再次校验权限。
- 审计日志必须记录：自动回复、审核通过、修改、拒绝、发布、平台、操作者。

自动回复要自然、礼貌、简短，不能夸大宣传、使用极限词或虚假承诺。
